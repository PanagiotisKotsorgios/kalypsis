# Local-only compose override — publishes ports to the host so smoke tests
# can hit the API and web SPA directly. NOT for Coolify (which routes via
# its own reverse-proxy).
services:
  api:
    ports:
      - "8080:8080"
  web:
    ports:
      - "8081:80"
